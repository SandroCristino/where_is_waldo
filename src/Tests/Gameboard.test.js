import Gameboard from '../Components/Gameboard.js';

describe('buildMapList()', () => {
    // it('should set the mapList state with at least one image', () => {
    //   const gameboard = new Gameboard();
    //   gameboard.buildMapList();
    //     console.log(gameboard.state.mapList)
    //   expect(gameboard.state.mapList.length).toBeGreaterThan(0);
    // });
});